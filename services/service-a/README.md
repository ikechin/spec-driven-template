# service-a (placeholder)

<!-- TODO(claude): このディレクトリは Git サブモジュールのプレースホルダーです。
     実際のサービスを追加する手順:

     1. このプレースホルダー README を削除する
     2. 上位ディレクトリから以下を実行:

        git rm -rf services/service-a
        git submodule add <repo-url> services/<actual-service-name>

     3. CLAUDE.md / services/README.md 内の `service-a` を実名に置換

     役割（frontend / BFF / backend / worker 等）はプロジェクト依存で決めてください。 -->

サービス追加前のプレースホルダーです。詳細は [`../README.md`](../README.md) を参照してください。
