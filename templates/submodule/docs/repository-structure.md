# リポジトリ構造

<!-- TODO(claude): サブモジュール内のファイル/ディレクトリ構成と役割を記述します。 -->

## ディレクトリ構成

<!-- TODO(claude): 以下のコマンドを実行して結果を貼り付けること:
     `tree -L 2 -I 'node_modules|dist|build|.next|vendor|target'`
     深さ 2 で十分。重要なディレクトリのみに整理してもよい。 -->

```
<service-name>/
├── TODO
└── TODO
```

## 各ディレクトリの役割

<!-- TODO(claude): 上記ツリーの主要ディレクトリそれぞれについて 1 行で役割を説明。
     例:
     - `src/controllers/` — HTTP リクエストハンドラ
     - `src/services/` — ビジネスロジック層
     - `src/repositories/` — データアクセス層
     - `tests/` — ユニットテスト・統合テスト -->

## ビルド・実行コマンド

<!-- TODO(claude): package.json scripts / Makefile / justfile / go の慣習などから抽出。
     最低限、以下をカバーすること:
     - 依存インストール
     - 開発サーバ起動
     - ビルド
     - テスト実行
     - lint / format -->

```bash
# 依存インストール
TODO

# 開発サーバ起動
TODO

# テスト実行
TODO
```
