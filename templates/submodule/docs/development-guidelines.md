# 開発ガイドライン

<!-- TODO(claude): このサービスにおけるコーディング規約・テスト規約・横断的要件の実装方法を記述します。
     親リポジトリの docs/ にある横断的要件を、このサービスでどう実装するかを明示すること。 -->

## コーディング規約

<!-- TODO(claude): 以下を実調査して埋める。
     - 言語標準のスタイルガイドへの準拠 (例: gofmt, prettier, black)
     - lint 設定ファイル (.eslintrc, .golangci.yml, ruff.toml 等) の存在と主要ルール
     - 命名規則 (camelCase / snake_case / PascalCase の使い分け)
     - import / package 構成のルール
     - コメント言語 (日本語/英語)
     既存ソースを 2-3 ファイル読んで実態に合わせること。 -->

## テスト規約

<!-- TODO(claude): 以下を埋める。
     - 使用するテストフレームワーク
     - テストファイルの配置場所 (`*_test.go`, `__tests__/`, `*.spec.ts` etc.)
     - カバレッジ目標 (任意)
     - モック/スタブの方針
     - E2E テストとの責任分界 (E2E は親リポの e2e/ で実施) -->

## セキュリティ実装

親リポジトリの [security-guidelines.md](../../docs/security-guidelines.md) で定義された要件を、このサービスでは以下の方法で実装します。

<!-- TODO(claude): 該当する項目について実装方法を記述。
     - 認証 / 認可の実装箇所 (middleware, guard, interceptor)
     - 入力バリデーション (どのライブラリで、どこに書くか)
     - 機密情報の扱い (環境変数、シークレットマネージャ)
     - SQL インジェクション / XSS / CSRF 対策の実装方法
     既存実装が無い場合は「未実装。次フェーズで対応」と書いてよい。 -->

## J-SOX 対応の実装方法

親リポジトリの [jsox-compliance.md](../../docs/jsox-compliance.md) で定義された要件を、このサービスでは以下の方法で実装します。

<!-- TODO(claude): 該当する項目について実装方法を記述。
     - 監査ログの出力箇所と形式
     - 職務分掌 (SoD) の判定ロジック
     - 承認フローへの関与
     - データ変更履歴の保持方法
     このサービスが Frontend のみで監査対象でない場合は「該当なし（参照のみ）」と明記。 -->
